import React from 'react';
import { AdminAuthForm } from '@/components/auth/AdminAuthForm';

/** /auth/admin — Admin sign-in only portal */
export default function AdminAuthPage() {
  return <AdminAuthForm />;
}
