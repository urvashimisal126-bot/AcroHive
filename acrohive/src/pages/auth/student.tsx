import React from 'react';
import { StudentAuthForm } from '@/components/auth/StudentAuthForm';

/** /auth/student — Student login + signup portal */
export default function StudentAuthPage() {
  return <StudentAuthForm />;
}
